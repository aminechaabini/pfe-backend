package com.example.demo.runner.old_files_for_reference;

public class e2eRunner {

    public void run(Object e2eRunRequest){


        // extracions variables
        // build items[1].request
        // call request
        // assert
        // extract (extracions variables = extracations)
        //for (i in e2e runrequest.items)
            //  build items. request + resolve extractions variables
            // call request
            // assert + resolve extractions variables
            // extract (extracions variables = extracations)
    }
}


// resolving of project+ suite vars